# Local dev: http://localhost:3001
# Production (same domain via nginx/Caddy): leave empty so requests use relative paths
VITE_API_URL=http://localhost:3001
VITE_LIVEKIT_URL=wss://your-project.livekit.cloud
# Must match DEV_SECRET in apps/api/.env when deploying publicly
VITE_DEV_SECRET=
